So dá pra trabalhar com interfaces funcionais em Java, que são interfaces 
que possuem apenas um método abstrato.

Tem parametro e tem o corpo da lambda que pode ou nao retornar algo.

(parametros) -> { corpo da lambda }

(parametros) -> expressão  // quando é uma unica expressão, nao precisa das chaves 

